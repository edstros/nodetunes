<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %> | artists</title>
</head>
<body>
 <h1>Artists</h1>
  <table>
  <tbody><% artists.forEach(function (artist){ %>
     <tr>
      <td><%= artist.name %></td>
      <td><%= artist.style %></td>
      <td><%= artist.language %></td>
      <td><%= artist.nationality %></td>
      <td><%= artist.bio %></td>
      <td><%= artist.members %></td>
      <td><%= artist.wiki %></td>
    </tr>
  </tbody>
  <% }); %>
  </table>
</body>
</html>
